# PHP TCP proxy target.
# Sourced by lib.sh::start_proxy. Must export all env vars the proxy needs
# and set BIN to the command line that launches it.

PROXY_ROOT_ENV="$(cd "$(dirname "${BASH_SOURCE[0]}")/../../.." && pwd)"

export TCP_BACKEND_ENDPOINT=127.0.0.1:15432
export TCP_POSTGRES_PORT=25432
export TCP_MYSQL_PORT=0
export TCP_SKIP_VALIDATION=1
export TCP_WORKERS=4

export BACKEND_TCP_PORT=15432

if [[ -n "${PROXY_HOST:-}" ]]; then
    BIN="php ${PROXY_REMOTE_DIR:-/opt/utopia-proxy}/bin/proxy tcp"
else
    BIN="php ${PROXY_ROOT_ENV}/bin/proxy tcp"
fi
